# Core dependencies
livekit-agents[openai,turn-detector,silero,cartesia,deepgram,aws,groq]~=1.2
livekit-plugins-noise-cancellation~=0.2.1
python-dotenv

# Optional: include dev dependencies (if needed for local/dev environments)
# pytest
# pytest-asyncio
# ruff
